---
name: trading-stock-scan
description: >
  对个股进行结构化深度研究，整合公告、机构评级、资金流向、技术面等多维信息。
  仅手动触发，不自动执行。
  触发词：深度扫描、研究一下、XXX怎么样。
---
⚠️ **OneDrive 同步提醒**：本 skill 写入的 Vault 位于 OneDrive，大量/频繁写入可能触发同步延迟与文件锁，建议分批操作。
# 个股深度扫描

## 触发条件

用户说以下任一短语时触发：
- "深度扫描 XXX"
- "研究一下 XXX"
- "XXX 怎么样"
- "帮我看看 XXX"

仅手动触发，不在复盘或盘前预测中自动执行。

## 数据源

全部免费、无需 API Key：

| 数据 | 来源 | 获取方式 |
|------|------|----------|
| 实时行情 | 东方财富 API | `push2.eastmoney.com` |
| 近期公告 | 东方财富 | `data.eastmoney.com/notices/stock/{code}.html` |
| 机构评级 | 东方财富 | `data.eastmoney.com/report/stock/{code}.html` |
| 资金流向 | 东方财富 API | 已有字段 f62, f184 |
| 板块同行表现 | 东方财富 API | 板块资金流向 API |
| 最新财务数据 | 东方财富 | `emweb.securities.eastmoney.com` |
| 舆情/新闻 | web search | `"{股票名}" site:eastmoney.com OR site:cls.cn OR site:10jqka.com.cn` |

## 数据双源校验（已安装 dsh-xueqiu 插件后适用）

- **行情/技术面**：主用东方财富（口径基准），用 `xueqiu_quote`（实时行情）+ `xueqiu_kline`（K线可视化，对话内直接出图）**交叉复核**。
- **舆情/热议**：`xueqiu_kol`（个股热议大V）+ `xueqiu_news`（7×24 快讯）**补充** web search，覆盖东财/财联社之外的社区情绪面。
- **代码/标的名定位**：`xueqiu_search`（英文名/拼音/代码互查），在 `{VAULT_PATH}/wiki/entities/` 找不到对应 entity 时优先用它确认代码。
- **冲突规则（硬性）**：两源数值不一致 → 在扫描报告"基本面快照 / 技术面"中**显著标注**（如 `⚠️ 数据冲突：东财 vs 雪球`），**不自动取信任一**；估值/财务类以东方财富口径为准。
- 写入扫描文件可用 `obsidian_write`（已装 dsh-obsidian 时），参考 obsidian-vault-sync 的工具优先级。

## 执行流程

### Step 1: 确定标的

- 用户提供股票名称或代码
- 若提供名称，在 `{VAULT_PATH}/wiki/entities/` 中查找对应 entity 文件获取代码
- 若未找到，通过 web search 确认代码

### Step 2: 并行获取数据

同时执行以下查询：

1. **实时行情**：调用东方财富 API 获取最新价、PE、PB、市值
2. **近期公告**：`curl` 抓取东方财富公告页 `data.eastmoney.com/notices/stock/{code}.html`，用 `grep`/`awk` 提取近30天公告标题和类型
3. **机构评级**：`curl` 抓取东方财富研报页 `data.eastmoney.com/report/stock/{code}.html`，提取近期评级和目标价
4. **资金流向**：调用东方财富 API 获取近5日主力净流入/出
5. **财务数据**：`curl` 东方财富财务页，或调用东方财富 F10 API 获取近4季度关键指标
6. **舆情新闻**：`tool-web` search 搜索近期相关新闻

> 说明：DSH 无 `webfetch` 工具。东方财富公告/研报/财务页可 `curl` 抓取 HTML 后以文本方式解析；结构化数据优先走东方财富 API。

### Step 3: 整合分析

将获取的数据整合为结构化报告。

### Step 4: 关联现有持仓

读取 `{VAULT_PATH}/交易体系/交易计划/当前持仓.md`：
- 若标的在持仓中 → 标注当前逻辑状态，分析是否需要更新
- 若标的在待入场计划中 → 标注入场条件完成度变化
- 若不在持仓/计划中 → 标注"观察中"

### Step 5: 写入文件

输出路径：`{VAULT_PATH}/wiki/entities/{股票名}-深度扫描-{YYYY-MM-DD}.md`

## 输出格式

```markdown
---
type: scan
date: {YYYY-MM-DD}
tags: [个股扫描, {板块}]
related: ["[[{股票名}]]", "[[{板块主题}]]"]
sources: ["eastmoney", "web-search"]
---

# {股票名}({代码}) 深度扫描

## 基本面快照

| 指标 | 数值 | 行业均值 | 判断 |
|------|------|----------|------|
| 最新价 | {值} | - | - |
| PE(TTM) | {值} | {均值} | 偏高/合理/偏低 |
| PB | {值} | {均值} | 偏高/合理/偏低 |
| 市值 | {值} | - | 大/中/小盘 |
| ROE | {值} | {均值} | - |
| 负债率 | {值} | {均值} | - |

### 近4季度业绩趋势
| 季度 | 营收(亿) | 同比增速 | 净利润(亿) | 同比增速 |
|------|----------|----------|------------|----------|
| {Q} | {值} | {值} | {值} | {值} |

## 近期公告（近30天）

| 日期 | 标题 | 类型 |
|------|------|------|
| {日期} | {标题} | 利好/利空/中性 |

## 机构动态

- **近期评级**：{评级}（{机构}，{日期}）
- **目标价范围**：{最低} - {最高}（当前价 {当前}）
- **近30天研报数量**：{数量}
- **核心观点摘要**：{摘要}

## 资金面

| 日期 | 主力净流入(万) | 净流入占比 |
|------|---------------|-----------|
| {日期} | {值} | {值}% |

- **近5日趋势**：{净流入/净流出}趋势
- **北向资金**：{持仓变化}
- **与板块对比**：{板块}近5日 {净流入/净流出}，个股 {跑赢/跑输}板块

## 技术面（简）

| 指标 | 数值 | 判断 |
|------|------|------|
| 近5日涨跌幅 | {值}% | - |
| 近20日涨跌幅 | {值}% | - |
| 近5日日均成交额 | {值}亿 | 放量/缩量/持平 |
| 振幅(今日) | {值}% | - |

## 风险提示

- {风险因素1}
- {风险因素2}
- 需关注节点：{事件}

## 与当前持仓/计划的关联

- **持仓状态**：{在持仓中/在待入场计划中/观察中}
- **逻辑状态**：✅/⚠️/❌（如在持仓中）
- **建议**：{继续持有/关注/可考虑入场/建议减仓}
```

## 格式规范

- 数值保留2位小数
- 金额单位：亿（成交额、市值）/ 万（主力净流入）
- 使用 Obsidian 兼容的 Markdown 语法
- frontmatter 中 `related` 必须链接到对应的 entity 文件
